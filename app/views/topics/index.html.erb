<div class="container">
<h1>Jamarks</h1>
  <%= link_to "New Topic", new_topic_path, class: 'btn btn-success' %>
  
    <div class="row">
      <% @topics.each do |topic| %>
        <%= render partial: 'users/topic', locals: {topic: topic, bookmarks: topic.bookmarks} %>
      <% end %>
    </div>


</div>

