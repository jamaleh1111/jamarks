%h1= @topic.title

= link_to "New Bookmark", new_topic_bookmark_path(@topic), class: 'btn btn-success'

%p
  - @bookmarks.each do |bookmark|
    %p= link_to bookmark.url

    = link_to "Delete Bookmark", [bookmark.topic, bookmark], method: :delete, class: 'btn btn-danger'
 
